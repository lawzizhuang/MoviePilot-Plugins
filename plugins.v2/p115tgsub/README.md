# 115 TG 订阅追更（P115TGSub）

MoviePilot V2 插件：读取 MoviePilot 待处理订阅（状态 `N`、`R`），检索 Telegram 公开频道中的 **115 与夸克** 分享资源及 4K Monitor 匿名免费磁力候选，校验分享目录与缺集后转存缺失的电影和剧集；夸克转存成功后联动 SmartStrm 在本地增量生成 STRM。

## v2.4.8

- **电影身份校验**：Telegram 候选仅以资源标题区匹配片名，剧情简介不再参与；电影文件还须在文件名或资源目录中明确命中目标片名，避免简介关键词或分享内无关大文件造成错片转存、误完成订阅。

## v2.4.7

- **离线候选链路修复**：补齐 Telegram 正文 ED2K / Magnet 搜索的追更排序参数传递；115 分享未命中时可正常继续离线资源检查，不再抛出参数错误。

## v2.4.6

- **追更候选最新优先**：识别到已完整拥有前序集、仅连续缺少季末集的追更订阅时，按 Telegram 消息发布时间从新到旧排序后再截取每频道候选；老剧补档不按日期淘汰历史消息。明确命中待补集的消息始终优先。

## v2.4.5

- **超前点映追更**：TMDB 剧集播出日期仅作为元数据参考，不再提前拦截 Telegram、115 或夸克检索；超前点映、会员抢先看或 TMDB 元数据滞后时，实际存在的候选仍可进入严格的标题、季集和分享目录文件校验。

## v2.4.4

- **115 Web 查询容错**：对 405、5xx 与网络临时异常的只读查询仅退避重试一次；明确区分查询暂时失败与分享失效，不再将 405 误记为失效。连续 3 次 405 后停止后续 115 Web 查询，下轮自动恢复。
- **搜索收敛**：仅当电视剧待补集数不超过 3 集时启用 `SxxExx` 定向 Telegram 检索，整季缺失保持标题泛搜索。

## v2.4.3

- **Telegram 连载缺集优先**：电视剧存在待补集时，先按 `片名 SxxExx` 定向检索（最多前三个缺集），并在每频道消息上限前优先排序明确命中待补集的候选，避免旧集占满候选而漏掉后续新集。

## v2.4.2

- **设置页重构**：按运行与安全、115 凭据与目录、Telegram、离线磁力、夸克和 SmartStrm 分区；来源配置折叠展示，保留全部原有配置项及默认值。

## v2.4.1

- **详情页重构**：将长段文本状态改为运行概览指标卡、来源与离线队列标签、追更操作区、最近转存记录表及独立的订阅进度核验区。

## v2.4.0

- **4K Monitor 自动排查**：每轮按 MoviePilot 已确认 TMDB ID 检索匿名免费候选；电影与标题明确匹配的剧集资源可进入既有 115 离线队列。
- **免费与访问边界**：只接受 `free`、`credit_cost=0`、未锁定且允许访问的候选；不登录、不使用 Cookie、不消耗会员/credits、不调用解锁接口。403/429 立即熔断本轮后续请求。

## v2.3.5

- **核验结果可视化**：订阅进度核验改用统计卡片与差异表格；将连续集数压缩展示，按列清晰呈现 Emby 已入库、MoviePilot `note` 修复前后和缺失集数变化。

## v2.3.4

- **订阅进度核验 / 修复**：详情页新增“只读预览订阅进度差异”与“确认修复预览中的订阅进度”。二者只读取 Emby 已入库的电视剧季集和 MoviePilot 当前订阅；预览不写数据，确认修复只补充 `note` 并重算 `lack_episode`，绝不回退进度。
- **完全隔离追更链路**：核验/修复不会搜索 Telegram/SeedHub，不访问 115 或夸克分享，不创建目录、不转存、不提交 ED2K/Magnet 离线任务、不触发 SmartStrm，也不写下载历史。
- **全量当前订阅**：不局限于 `N`、`R` 待处理状态，可安全核验此前双网盘并行遗留的当前订阅；仅以 Emby 已确认集数作为写入事实，媒体库无确认集数时保持订阅原样。

## v2.3.3

- **媒体库事实同步订阅**：已入库剧集会回写 MoviePilot 订阅的 `note` 与缺失集数，并从回退补档范围剔除，避免重复搜索、离线下载和整理。
- **Telegram 频道诊断**：初始化和同步启动时展示已加载频道；115/夸克分享、ED2K/Magnet 搜索完成时展示实际请求频道数量，便于核验配置是否完整生效。

## v2.3.2

- **115 回补以目录真值为准**：旧夸克成功历史不再被视为 115 文件存在；删除夸克文件、关闭夸克并重置订阅后，可重新搜集并保存至 115。
- **SeedHub 限流熔断**：首次返回 HTTP 403/429 时停止本轮后续 SeedHub 页面请求，避免连续扫描扩大限流；下轮再尝试。

## v2.3.1

- **SeedHub 直连优先**：SeedHub 公开页面默认不复用 Telegram HTTP 代理，降低代理出口触发访问限制的概率；容器直连失败时可在配置页按需开启代理。

## v2.3 能力

- **4K Monitor 匿名免费磁力**：按 MoviePilot 已确认的 TMDB ID 精确查询 `4kmonitor.org`；每轮自动排查，仅接受 `free`、`credit_cost=0`、未锁定且允许访问的候选。电影及标题明确匹配当前季集的电视剧资源可进入既有 115 离线队列。
- **4K Monitor 安全边界**：不登录、不使用 Cookie、不调用会员/credits 解锁接口；磁力仅在最终免费候选上解析一次，403/429 本轮熔断，默认直连并以低频节流访问。

- **SeedHub 公开磁力**：通过指定 Telegram 公开频道（默认 `seedhub_pro`）定位 `sidhub.cc/movies/<ID>/`，读取公开磁力列表并解码同源 `link_start` 页的 Magnet，提交到既有 115 电影或电视剧季目录。
- **完整季安全提交**：`全N集`、`EP01-N` 等清晰完整季候选只创建一个 115 云下载任务；以目标目录实际出现的视频文件逐集确认，绝不因任务提交或全集标题提前完成未播订阅。
- **范围边界**：不使用 SeedHub 登录、Cookie、验证码或浏览器自动化；不抓取其夸克/百度/迅雷等其他网盘标签，不支持 `.torrent`。

## v2.2 能力

- **115 离线下载**：支持 Telegram 公开消息正文直接包含的 ED2K / 磁力链接；提交时显式传入电影或电视剧季目录的 115 CID，不使用默认离线下载目录。
- **完成确认与兜底抑制**：提交成功仅进入待确认队列；目标目录出现匹配媒体文件后才更新订阅。下载中的电影或剧集不会进入夸克重复兜底，任务超时后自动释放。
- **范围边界**：仅处理 Telegram 正文直链；不抓取 SeedHub、sidhub.cc 或其他第三方资源页，不支持 `.torrent` 文件。

## v2.1 能力

- **夸克目录分页保护**：分享目录返回空页时立即停止分页，避免异常总数导致持续翻页阻塞同步。
- **脱敏失败分类**：夸克候选失败区分分享失效、访问码异常、访问受限、风控、网络或接口异常；不记录分享链接、访问码或响应原文。
- **运行状态卡**：详情页展示最近一轮订阅处理量、候选及去重数、夸克失败分类、115/夸克转存数、SmartStrm 触发/重试摘要和最近媒体状态。

## v2.0 能力

### 115 链路（稳定保留）

- 仅访问 Telegram 公开搜索页 `https://t.me/s/<频道>?q=<片名>`，不支持私有频道、邀请链接或登录会话；
- 支持消息内直接的 115 链接，以及 `lsp115` 式「查看资源」跳转到 Telegraph 后的 115 链接；
- 仅接受 `115.com`、`115cdn.com`、`anxia.com` 域名分享；
- 自动处理全部 MoviePilot 待处理订阅；媒体库无缺集明细时按订阅声明范围回退；
- 单字剧名与标题过滤均保持严格匹配；Telegram 候选先按订阅标题初筛、再应用单频道上限；
- 115 的 STRM 生成与 302 播放由「115网盘STRM助手（P115StrmHelper）」链路负责，本插件不介入。

### 夸克链路（v2.0 新增，115 无可用候选时兜底）

```text
订阅缺失
→ Telegram / Telegraph 提取 pan.quark.cn 分享候选（与 115 同一次消息解析）
→ 解析分享 ID / 提取码
→ 运行时复用 QuarkDisk Cookie 获取 stoken
→ 校验分享目录与标题/季/集/扩展名
→ 115 无可用候选时，指定文件转存至夸克个人媒体目录
→ 异步任务等待 + 目标目录二次确认文件真实存在
→ 更新 MoviePilot 订阅缺集/note
→ 触发 SmartStrm 增量生成本地 STRM（失败进入待重试队列）
```

关键约束：

- **115 优先、夸克兜底**：同一集已由 115 链路成功转存（历史记录可见）时，夸克链路直接跳过，绝不双盘重复转存；
- 夸克 Cookie 仅运行时读取 `QuarkDisk` 的 `cookie` 配置，不复制到本插件、不回显、不写日志；
- 夸克远端只保存媒体文件；`.strm`、NFO、海报等全部由 SmartStrm 在本地生成，本插件不调用 MoviePilot 远端刮削；
- 提取码、`stoken`、`share_fid_token` 只在运行内存短期保存，不持久化；
- 风控词（频繁/风控/限制/封禁）触发冷却熔断，仅网络瞬态异常重试；
- 转存成功以「目标目录二次确认文件存在」为准，任务提交不算数；
- 转存成功与 STRM 后处理解耦：SmartStrm Webhook 失败只入待重试队列（最多 N 次后停滞告警），**绝不重新发起网盘转存**。

### SmartStrm 联动

- Webhook 地址从 SmartStrm「系统设置-Webhook」获取；本插件采用与 Quark-Auto-Save 相同的调用契约：
  - `GET webhook` 校验连通性；
  - `POST webhook`，`{"event": ..., "data": {"strmtask": "任务名", "savepath": "转存目标目录", "xlist_path_fix": ""}}`；
- `savepath` 只传本次转存成功的目标目录，SmartStrm 仅对该目录做增量 STRM 生成；
- 夸克网盘驱动时无需填写 `xlist_path_fix`；OpenList 驱动时填「挂载路径:夸克根目录」；
- Webhook 地址属敏感凭据：仅存于 MoviePilot 配置，不写入日志、通知或 Git。

## 部署与配置

1. 安装并启用「夸克网盘存储」（`QuarkDisk`）并完成 Cookie 配置（夸克链路运行时复用）；
2. 在「Telegram 公开频道」填写公开频道用户名或链接，每行一个（默认 `QukanMovie` / `lsp115` / `vip115hot`）；
3. 配置独立的 115 与夸克电影/电视剧转存目录；
4. 如需使用 ED2K / 磁力：在公开频道列表添加直链资源频道并启用「115 ED2K / 磁力离线下载」；4K Monitor 默认每轮按 TMDB ID 排查匿名免费候选；如需 SeedHub，另行启用「SeedHub 公开磁力资源」（默认频道 `seedhub_pro`）；
5. 配置夸克链路和 SmartStrm 后处理；
6. 保持「测试模式」开启，先点详情页「立即运行一次」，核对日志中的 Telegram、4K Monitor、SeedHub、115/夸克候选校验与剧集匹配；
7. 用详情页「验证夸克连通性」「测试 SmartStrm」分别做只读验证；
8. 确认无误后关闭测试模式。

## 操作页面

- **最近运行状态**：展示上一轮脱敏统计与最近媒体状态；不是实时执行进度。
- **订阅进度核验**：先点击“只读预览订阅进度差异”，刷新页面后通过统计卡片和差异表格核对每条订阅的 Emby 已入库集数、`note` 修复前后及缺失集数变化；确认无误后点击“确认按当前媒体库修复订阅进度”。该操作不会触发任何搜索、网盘或 STRM 动作。
- **立即运行一次**：异步执行一次完整同步（115 优先，夸克兜底）；
- **验证夸克连通性**：只读验证 `QuarkDisk.cookie` 与账号连通性；
- **测试 SmartStrm**：只读 GET Webhook 校验连通性，不触发任务；
- **清理插件日志**：清空本插件日志，不影响主日志与其他插件。

## 敏感信息边界

以下信息不会被本插件打印、记录或提交到仓库：115/夸克 Cookie、MoviePilot API Token、SmartStrm Webhook 完整 URL（日志仅显示主机名）、分享提取码、`stoken`、`share_fid_token`、完整分享 URL。下载历史仅记录分享 ID 与目标目录。
